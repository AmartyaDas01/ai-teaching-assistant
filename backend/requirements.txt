# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17
pydantic-settings==2.6.1

# Database
# Pinned <2.0.36 for langchain-community 0.3.8 compatibility.
SQLAlchemy==2.0.35
# Postgres driver (used in Docker/production; SQLite needs no driver).
psycopg2-binary==2.9.10

# Auth
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
email-validator==2.2.0

# RAG / LLM
langchain==0.3.9
langchain-community==0.3.8
langchain-openai==0.2.10
langchain-chroma==0.1.4
langchain-ollama==0.2.1
chromadb==0.5.20
# Remote vector store (VECTOR_STORE=qdrant) - persists vectors off-disk, so they
# survive restarts on hosts with no persistent disk (free tiers).
qdrant-client==1.18.0

# Embeddings (local)
sentence-transformers==3.3.1

# Document parsing
PyMuPDF==1.24.14
python-docx==1.1.2
python-pptx==1.0.2
