# Test-only dependencies. Kept out of requirements.txt so they never reach the
# production image (the Dockerfile installs requirements.txt).
-r requirements.txt
pytest==8.3.4
httpx==0.28.1          # required by fastapi.testclient
